@model ContentManagementSystem.ApplicationCore.DTOs.TestimonialDto

@{
    ViewData["Title"] = "Chỉnh sửa nhận xét";
    Layout = "_Layout";
}

<div class="max-w-2xl mx-auto space-y-6">
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-2xl font-bold text-gray-800">Chỉnh sửa nhận xét</h2>
        </div>
        <a asp-action="Index" class="text-sm font-medium text-gray-600 hover:text-gray-900 bg-white px-4 py-2 rounded-xl border border-gray-200 shadow-sm transition-all">
            &larr; Quay lại
        </a>
    </div>

    <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 sm:p-8">
        <form asp-action="Edit" method="post" class="space-y-5">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedAt" />
            <div asp-validation-summary="ModelOnly" class="p-4 bg-rose-50 border border-rose-100 rounded-xl text-rose-600 text-sm"></div>

            <div>
                <label asp-for="CustomerName" class="block text-sm font-semibold text-gray-700 mb-1">Tên khách hàng <span class="text-rose-500">*</span></label>
                <input asp-for="CustomerName" class="w-full px-4 py-2.5 rounded-xl border border-gray-200 text-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent" required />
                <span asp-validation-for="CustomerName" class="text-xs text-rose-500 mt-1 block"></span>
            </div>

            <div>
                <label asp-for="Rating" class="block text-sm font-semibold text-gray-700 mb-1">Đánh giá (Số sao)</label>
                <select asp-for="Rating" class="w-full px-4 py-2.5 rounded-xl border border-gray-200 text-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-white">
                    <option value="5">★★★★★ (5 sao - Xuất sắc)</option>
                    <option value="4">★★★★☆ (4 sao - Rất tốt)</option>
                    <option value="3">★★★☆☆ (3 sao - Khá)</option>
                    <option value="2">★★☆☆☆ (2 sao - Trung bình)</option>
                    <option value="1">★☆☆☆☆ (1 sao - Kém)</option>
                </select>
                <span asp-validation-for="Rating" class="text-xs text-rose-500 mt-1 block"></span>
            </div>

            <div>
                <label asp-for="Content" class="block text-sm font-semibold text-gray-700 mb-1">Nội dung nhận xét <span class="text-rose-500">*</span></label>
                <textarea asp-for="Content" rows="4" class="w-full px-4 py-2.5 rounded-xl border border-gray-200 text-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent font-sans" required></textarea>
                <span asp-validation-for="Content" class="text-xs text-rose-500 mt-1 block"></span>
            </div>

            <div class="pt-4 border-t border-gray-100 flex items-center justify-end gap-3">
                <a asp-action="Index" class="px-5 py-2.5 rounded-xl border border-gray-200 text-gray-600 hover:bg-gray-50 text-sm font-medium transition-all">Hủy bỏ</a>
                <button type="submit" class="px-6 py-2.5 rounded-xl bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-semibold shadow-sm transition-all flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
                    Cập nhật
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

